APPLY docker://domainnamelookup ON traffic

github_traffic = traffic WHERE x_domain_organization LIKE '%GitHub%'
iana_traffic = traffic WHERE x_domain_organization LIKE '%IANA%'
amazon_traffic = traffic WHERE x_domain_organization LIKE '%Amazon%'
akamai_traffic = traffic WHERE x_domain_organization LIKE '%Akamai%'
cloudflare_traffic = traffic WHERE x_domain_organization LIKE '%Cloudflare%'